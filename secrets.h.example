#pragma once

// Copy this file to secrets.h and fill in your WiFi credentials.
// secrets.h is gitignored so your real credentials won't be committed.

#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"

// TODO: future — replace compile-time credentials with a WiFiManager
// captive-portal onboarding flow so users don't need to edit and reflash
// to change networks.
